# 第 393 期归档验证记录

验证日期：2026-09-06。平台：macOS / Apple Silicon，Node.js **22.23.2**，npm **10.9.8**。验证对象是本目录中的分享版本，验证时未复制源实验的依赖、构建产物和运行存档。

本文记录可运行性检查，不作为模型评分或限时挑战的原始成绩。未在 Windows / Linux 上实际执行，也未对任意用户输入和仓库地图进行穷尽验证。

## 安装、测试与构建

在两个案例各自的 `gpt-astra/` 目录重新执行：

```bash
npm ci --no-audit --no-fund
npm test
npm run build
```

| 验证项 | Folio 表格 | Relay 仓库 |
| --- | --- | --- |
| 从锁文件全新安装 | 通过，174 个安装包 | 通过，150 个安装包 |
| 自动化测试 | **19 / 19 通过** | **11 / 11 通过** |
| TypeScript 检查 | 通过，包含在 build 中 | 通过，包含在 build 中 |
| 前端生产构建 | Vite 6.4.3 通过 | Vite 6.4.3 通过 |
| 后端生产构建 | esbuild 通过 | esbuild 通过 |
| 开发命令 `npm run dev` | 前端 5173、后端 3001 启动成功 | 3000 端口启动成功 |
| 生产命令 `npm start` | 3001 端口启动成功 | 3000 端口启动成功，使用 `--production` |

所有测试均无跳过、失败或取消。安装包数量来自本次 macOS 环境，其他平台的可选依赖数量可能不同。

表格测试覆盖手写公式解析、优先级与嵌套函数、增量依赖重算、循环及错误传播、撤销重做、百分号、ROUND、IF，以及真实 HTTP 服务重启后的保存恢复。

仓库测试覆盖固定地图、12 个并发运输任务完成过程中的顶点碰撞和对穿检查、动态封路及恢复、单格通道的相向运输、任务优先级老化、状态持久化、SSE 快照和事件序号、HTTP 创建任务及服务器重启。

## 实际浏览器检查

使用 Ego Lite 的 `ego-browser` 在本地页面操作，并结合 DOM 和本地 API 核对结果。

### Folio

- 开发页面显示完整的 40 × 12 网格，共 480 个单元格。
- 在空白的 B1:E1 建立 `2 → =B1+1 → =C1*2 → =D1+1`，结果为 `2, 3, 6, 7`。
- 将 B1 改为 `5`，结果依次更新为 `5, 6, 12, 13`。
- 将 B1 改为 `=D1`，B1:D1 的循环和 E1 的依赖传播均显示 `#CYCLE!`。
- 点击撤销恢复 `5, 6, 12, 13`，重做恢复循环报错；再次撤销并刷新后数值和公式保留。
- 停止开发服务、使用构建后的后端运行 `npm start` 后，生产页面仍显示保存的值，API 读取也一致。

### Relay

- 开发页面成功连接 SSE，从默认 4 个任务开始，通过界面连续创建 3 个任务，任务总数变为 7。
- 单步后 tick 从 0 变为 1；运行并暂停后 tick 继续增长，暂停状态正确。
- 点击机器人下一步的可通行格子 `(3, 4)`，后端立即记录障碍，全部剩余路线都避开该格；再次点击后解除障碍。
- 刷新后仍显示 7 个任务、tick 3、序号 10，并重新显示“系统已连接”。
- 停止开发服务、通过 `npm start` 启动生产版本，任务、tick 和序号保持一致。
- 生产页面的地图全屏和退出全屏均正常响应。

两份生产页面的 HTML、打包后的 JavaScript / CSS 资源及 API 均成功返回；页面使用 `/assets/` 中的构建资源，不依赖 Vite 开发客户端。浏览器截图仅用于本次本机核对，不收录进案例。

## 文件完整性与 Git 范围

- 两份 `prompt.md` 均与源 `PROMPT.md` 按字节一致。
- Folio 的前后端、共享模块和测试源码保持一致；Relay 源码仅修改生产模式入口，使其识别 `--production`，未改动调度算法和界面。
- 两份锁文件中的依赖包版本、integrity 和依赖结构保持一致；仅替换 registry 下载域名，并在根包元数据增加 Node.js 版本要求。
- 使用 Git 的忽略规则检查可收录文件，确认所有必需源码、默认示例和锁文件均可收录。
- 检查运行后生成的 `data/`、`node_modules/`、`dist/`、`dist-server/`，以及模拟的环境变量、密钥和 Agent 本机状态路径，均被忽略。
- 对本期可收录文件检查常见 API Token、私钥头、带凭据的 URL 和本机用户目录路径，未发现匹配。

| 提示词 | SHA-256 |
| --- | --- |
| Case 01 | `44bf06dcf8fefdf3f6d11cbbf2c109b3a62ccdf4210b82135ce86772e36f3dbb` |
| Case 02 | `af9450ec0845f42bf27e7adca006a0c4684898dc1018f98ea33fa566679a0f83` |

后续复测可按本期 README 和各项目 README 操作。
