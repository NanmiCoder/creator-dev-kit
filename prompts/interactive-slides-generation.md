# 交互式演示网站 — AI 生成提示词

> 本文档是生成交互式 PPT 演示网站的完整提示词模板，包含技术栈、设计规范、页面结构和文案内容。
> 配套视频演示地址：https://dakou.iflow.cn/share/641293b532b341d4a7e0b29639e9ca4f

---

## 你要做什么

用 React + Vite + Tailwind CSS v4 + Framer Motion + Lucide React 搭建一个**全屏左右切页的交互式演示网站**（类似 PPT，但跑在浏览器里）。

## 技术栈

- React 19 + Vite（端口自定）
- Tailwind CSS v4（通过 `@tailwindcss/vite` 插件接入，CSS 文件顶部 `@import "tailwindcss"`）
- Framer Motion（页面切换动画 + 元素入场动画）
- Lucide React（图标库）

## 全局设计规范

### 配色方案

用 CSS 变量统一配色，所有组件通过 `var(--xxx)` 引用，方便一键换肤：

```css
:root {
  --primary-100: #019b98;   /* 主色 */
  --primary-200: #55ccc9;   /* 主色浅 */
  --primary-300: #c1ffff;   /* 主色最浅 */
  --accent-100: #dd0025;    /* 强调色 */
  --accent-200: #ffbfab;    /* 强调色浅 */
  --text-100: #014e60;      /* 正文深色 */
  --text-200: #3f7a8d;      /* 正文浅色 */
  --bg-100: #fbfbfb;        /* 背景 */
  --bg-200: #f1f1f1;        /* 背景次级 */
  --bg-300: #c8c8c8;        /* 背景最深 */
}
```

> AI 配色工具推荐：https://www.bairesdev.com/tools/ai-colors

### 视觉风格要素

1. **毛玻璃卡片**（glass-card）：`background: rgba(255,255,255,0.7)` + `backdrop-filter: blur(12px)` + 半透明边框
2. **光晕背景**（glow-bg）：绝对定位的大圆形色块，`filter: blur(100px)`，半透明，营造氛围感
3. **网格底纹**：用 `linear-gradient` 画 40px 间距的浅色网格线作为全局背景
4. **颜色渐变横幅**：页面底部的结论性文字用渐变色或强调色的圆角大卡片承载

## 页面切换机制

- 使用 Framer Motion 的 `AnimatePresence` + `mode="wait"` 实现左右滑入滑出
- 切换时根据方向（direction）决定从左还是右进入/退出
- 动画用 spring 弹簧（`stiffness: 300, damping: 30`）

```jsx
const slideVariants = {
  enter: (direction) => ({ x: direction > 0 ? '100%' : '-100%', opacity: 0, scale: 0.95 }),
  center: { x: 0, opacity: 1, scale: 1 },
  exit: (direction) => ({ x: direction < 0 ? '100%' : '-100%', opacity: 0, scale: 0.95 })
};
```

## 导航交互

- **键盘**：← → 和空格键切页
- **触控**：监听 touchstart/touchmove/touchend，滑动距离 > 50px 触发切页
- **底部控制栏**：左右箭头按钮 + 圆点指示器（当前页为拉长的胶囊形），点击圆点可跳转
- **顶部进度条**：细长条，宽度 = `(当前页 / 总页数) * 100%`
- **页码显示**：右上角 `01 / 10` 格式

## 每页幻灯片的结构模式

每个 Slide 是一个独立组件，遵循统一的动画容器模式：

```jsx
<motion.div variants={containerVariants} initial="hidden" animate="show" className="h-full flex flex-col">
  {/* 可选：glow-bg 光晕装饰 */}
  <TitleArea title="主标题" subtitle="副标题（可选）" />
  <div className="flex-1 flex flex-col items-center justify-center gap-8 pb-8">
    {/* 内容卡片区域 */}
    <ConclusionBanner text="一句话总结" />
  </div>
</motion.div>
```

子元素使用 `staggerChildren: 0.15` 依次入场，每个子元素用 `itemVariants`（从下方 20px 淡入）。

## 常见的内容布局

1. **三列特性卡片**：`grid grid-cols-3`，每张卡片有图标 + 标题 + 描述
2. **四步流程线**：`grid grid-cols-4`，顶部圆形步骤编号 + 下方卡片，中间用进度条连接
3. **两列问题列表**：`grid grid-cols-2`，左侧红色边框 + 警告图标 + 文字
4. **横向流程图**：一行 4 个图标方块，中间用箭头 `>` 连接
5. **纵向清单**：竖排大卡片，左侧圆形图标 + 右侧标题描述

## 项目结构

```
src/
  main.jsx
  App.jsx              # 导航状态、切页逻辑、整体布局
  index.css            # Tailwind 入口 + CSS 变量 + 自定义样式类
  animations.js        # slideVariants / itemVariants / containerVariants
  components/
    ui/
      TitleArea.jsx          # 标题 + 副标题
      ConclusionBanner.jsx   # 底部结论横幅
      Icons.jsx              # 自定义 SVG 图标（如果 Lucide 没有的话）
    slides/
      Slide1.jsx ~ SlideN.jsx  # 每页一个独立组件
```

## 关键注意事项

- `body` 设置 `overflow: hidden` 防止原生滚动
- 内容区域使用 `flex-col items-center justify-center` 让内容垂直居中，避免大片空白
- 结论横幅不要用 `mt-auto`（会被推到最底部），让它紧跟内容自然排列
- 所有幻灯片用 `absolute inset-0` 定位在同一容器内，靠 AnimatePresence 切换
- 首尾页 disable 对应的翻页按钮

---

# 每页幻灯片文案

> 本节定义了每一页的完整文案内容，供 AI 生成代码时直接引用。
> 格式：标题 → 副标题 → 内容模块 → 结论横幅。

---

## 第 1 页 — 封面 / 引言

- **标签**：Next-Gen Presentation
- **主标题**：交互式演示网站怎么做？
- **副标题**：不要把讲稿堆上屏幕，用视觉引导观众的注意力。

**三列卡片：**

| 图标 | 标题 | 描述 |
|------|------|------|
| MousePointerClick | 能交互 | 打破单向输出，支持复杂状态演示与点击反馈 |
| MonitorPlay | 能展示 | 全屏沉浸感，高分辨率下依然保持像素级完美 |
| Link | 能分享链接 | 摆脱笨重源文件，随时随地一个链接即可访问 |

---

## 第 2 页 — 需求定义

- **主标题**：需求定义：你真正要的，其实就三件事

**三列卡片：**

| 图标 | 标题 | 描述 |
|------|------|------|
| Layers | 像 PPT 一样有结构 | 多页面、幻灯片式的物理结构，支持线性叙事 |
| Settings | 像网站一样能交互 | 不再是截图，可以嵌入按钮、动效、实时交互 |
| Box | 像产品一样能交付 | 部署上线分享链接，而不是发文件发压缩包 |

- **结论横幅**：你要的不是排版，是交付体验

---

## 第 3 页 — 传统流程

- **主标题**：过去常用方案：先打底，再精修
- **副标题**：传统流程中的断点，往往是效率的黑洞

**四步流程线：**

| 步骤 | 标题 | 描述 |
|------|------|------|
| 01 | AI 生成雏形 | 使用工具生成静态结构 |
| 02 | 拉取本地代码 | 配置环境准备二开 |
| 03 | 精修组件动效 | 补充逻辑与交互细节 |
| 04 | 配置部署上线 | 寻找服务器挂载分享 |

---

## 第 4 页 — 痛点分析

- **主标题**：旧方案的问题：链路不完整
- **副标题**：问题不是做不出来，而是中间环节消耗了太多精力

**两列问题列表（红色警告）：**

1. 订阅叠订阅，多工具切换长期成本高
2. 本地能跑，不等于能在浏览器里直接交付给客户
3. 分享前还要额外补一轮 CI/CD 和部署配置
4. 流程断点多，修改反馈慢，效率被严重吃掉

- **结论横幅（红色强调）**：真正卡住你的，是整条链路顺不顺

---

## 第 5 页 — 新方案

- **主标题**：现在的新思路：端到端闭环
- **副标题**：将开发到分享压缩在同一个语境下完成

**横向流程图（四步，箭头连接）：**

| 图标 | 标题 |
|------|------|
| FileText | 自然语言开发 |
| Play | 在线即时测试 |
| Rocket | 一键部署上线 |
| Link | 直接分享链接 |

- **结论横幅**：你不是在做代码，你是在做交付

---

## 第 6 页 — 案例引入

- **标签（红色）**：真实项目拆解
- **主标题**：用真实案例去讲，最容易复用

**三列信息卡片：**

| 标签 | 值 | 图标 |
|------|------|------|
| 案例类型 | 交互式演示网站 | MonitorPlay |
| 使用场景 | 产品发布 / 内容讲解 | Presentation（自定义） |
| 讲解方式 | 全屏展示 · 左右滑动 | SlidersHorizontal（自定义） |

---

## 第 7 页 — 实操第一步：需求输入

- **主标题**：第一步：需求输入
- **副标题**：不要模糊描述，结构化的输入决定了生成的下限

**三列特性卡片：**

| 图标 | 标题 | 描述 |
|------|------|------|
| LayoutTemplate | 框架与模板 | 明确是一页一屏、左右滑动，还是长图滚动，限定基础物理结构。 |
| FileText | 业务文案拆解 | 精确到每一页讲什么、需要几个模块承载，而不是给一大段说明书。 |
| Palette | 视觉与配色规范 | 提供明确的 CSS 变量或十六进制色值，统一品牌感和信息层级。 |

- **结论横幅**：第一版质量 = 输入质量

---

## 第 8 页 — 实操第二步：先出能跑版本

- **主标题**：第二步：先出能跑版本
- **副标题**：控制预期，第一步永远是把架子搭起来，而不是雕花

**纵向清单（三项）：**

| 图标 | 标题 | 描述 |
|------|------|------|
| ShieldCheck | 基础运行正常 | 页面没有白屏报错，React 组件能正常渲染，样式初步加载。 |
| ShieldCheck | 物理结构完整 | 10 页的框架都在，导航、轮播、网格布局等基础容器已占位。 |
| ShieldCheck | 核心交互联通 | 键盘左右键能切页，按钮能点，核心的流转逻辑没有断点。 |

---

## 第 9 页 — 实操第三~五步：迭代、测试、上线

- **主标题**：第三至第五步：拉开差距的后半段
- **副标题**：真正的质感，来源于跑通基础后的快速迭代与在线测试

**三列带清单卡片：**

### 第一列 — 迭代修改（蓝色系）
- 图标：RefreshCw
- 清单项：
  - 调整动效曲线 (Framer Motion)
  - 优化卡片阴影与层级
  - 补充缺失的高亮色彩

### 第二列 — 在线测试（紫色系）
- 图标：MonitorPlay
- 清单项：
  - 全屏展示效果确认
  - 响应式布局检查
  - 键盘/触控交互复测

### 第三列 — 上线分享（绿色系）
- 图标：Globe
- 清单项：
  - 一键生成访问链接
  - 随时随地打开演示
  - 无需发文件发压缩包

---

## 第 10 页 — 结尾

- **主标题**：先别做大工程
- **副标题**：将复杂的开发动作，简化为可控的输出流

**标签云：**
- 大幅降低开发成本
- 流程端到端完整
- 所见即所得的交付
- 极低的使用门槛

**底部渐变大卡片（深色背景）：**

> "先跑通第一条**完整链路**"

（"完整链路"四字用 `primary-200` 高亮）
